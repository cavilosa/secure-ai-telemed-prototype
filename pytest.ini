# This is a configuration file for the pytest testing framework.
[pytest]

# This line tells pytest to add a directory
# to Python's path so it can find your modules.
# The "." means "add the current directory (the project root) to the path."
# This allows tests in tests/ to successfully import code from services/.
pythonpath = .